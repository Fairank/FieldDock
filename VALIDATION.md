# 验证与打样顺序

先运行 `python tools/verify_artifacts.py`。它核对发布文件的哈希、检查报告中的数量、最终 PCB 对应关系及库文件来源，不启动 KiCad。K 版已有随包保存的原生检查结果；这个脚本负责检查证据一致性。

`python tools/verify_artifacts.py --release` 还要求 PCB 无未连接项、无 DRC 违规、原理图 ERC 通过，以及经过几何核对的制造文件存在。此处 release 仅指裸 PCB 工程样板的数字制造文件；未满足条件时必须失败。请同时阅读 `release-status.json`，不要把下载到工程文件等同于生产放行。

原生工程位于 `cad/FD-MAIN-01.kicad_pro`。建议使用对应报告记录的 KiCad 版本。重新填写地铜后做 DRC，再核对网络表、钻孔、阻焊、钢网、BOM 和贴装方向。当前电脑曾发生 KiCad CLI 访问冲突，保存的报告由单次隔离诊断产生；仓库验证脚本不会自动启动该程序。

基础驱动可按 `firmware/README.md` 编译并运行主机测试。已有 Cortex-M4 目标文件编译证据，不含完整可烧录镜像。完成 SDK、启动文件、链接脚本和外设初始化之后才可做上板调试。

实物顺序：

1. 装配前核对电源端口、保护器件方向、LGA/QFN 焊接和电源对地电阻。
2. 用限流台式电源从 AUX 口上电，确认保护阈值、各电压轨和默认关闭状态；逐个使能负载，记录电流与温升。
3. 验证两口同时接入、插拔和掉电时的电源切换及反向电流，再接 iPhone。手机和 AUX 的可用预算不能相加。
4. 完成调试器与基础固件通信后，分别验证 USB/BLE、microSD、GPIO 隔离、红外和 iButton。
5. 单独调谐 Sub-GHz、NFC 和 LF 天线/线圈，最后装壳并贴近手机复测。匹配网络的初始值不是已测得的最终值。
6. 手机端数据通道、App、RF 功能、EMC/ESD、外壳公差与固定方式须各自留实测记录。未验证项不能在作品说明中写为已实现或已认证。

整机用例须在本人设备或获得授权的测试环境中验证。仓库不包含网络干扰电路。
